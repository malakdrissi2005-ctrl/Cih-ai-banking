"""Router top-level : décide, avant tout traitement, si un message relève du
Banking Agent (`agents/agent1_faq/`, jamais modifié) ou du General Agent
(`agents/general_agent/`). Voir `conversational_understanding.py`, `router.py`.
"""
