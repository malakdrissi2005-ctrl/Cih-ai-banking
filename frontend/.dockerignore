# Contexte de build = frontend/ (voir docker-compose.yml). Exclut tout ce qui
# n'est pas necessaire au build Vite ou qui doit venir du conteneur lui-meme.

node_modules
dist
.git
*.log
.vscode
.DS_Store
